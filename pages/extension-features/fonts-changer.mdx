# Fonts Changer

Try 1000+ Google and local fonts across six categories — `sans-serif`, `serif`, `monospace`, `display`, `local`, and `handwriting`. Apply these fonts to the entire website, headings, paragraphs, or an HTML tag. Changes can be saved, copied, or exported for future reference.

<br />
<video
	autoPlay='autoplay'
	loop
	muted>
	<source
		src='/videos/fonts-changer.mp4'
		type='video/mp4'
	/>
</video>
